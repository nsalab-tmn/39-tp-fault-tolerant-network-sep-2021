testbed:
  credentials:
    default:
      username: admin
      password: Passw0rd!

devices:
  terminal_server:
    connections:
      cli:
        ip: '%ENV{CML_SSH_HOST}'
        port: '%ENV{CML_SSH_PORT}'
    credentials:
      default:
        password: '%ENV{CML_PASSWORD}'
        username: '%ENV{CML_USER}'
  dummy:
    connections:
      a:
        ip: localhost
